export default function AssistantPage() {
  return (
    <section className="mx-auto flex w-full max-w-header flex-col gap-3 px-4 py-4">
      <h1 className="font-heading text-lg font-semibold text-foreground">Assistant</h1>
      <p className="text-sm text-muted-foreground">
        Open the chat (bottom-right). When tools run, layout, color squares, and recipes render in the strip below the site
        header on any page—you do not need to stay on this route.
      </p>
    </section>
  );
}
